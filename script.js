// Function to redirect to the map page
function redirectToMapPage() {
    window.location.href = 'map.html';
}

// Delay the redirection
setTimeout(redirectToMapPage, 5000); // 5000 milliseconds (5 seconds)
